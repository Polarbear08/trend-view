{
  "name": "trend-view",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/index.js -o dist/bundle.js"
  },
  "author": "polarbear08",
  "license": "MIT",
  "dependencies": {
    "browserify": "^16.5.0"
  }
}